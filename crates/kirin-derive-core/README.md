# Core derive macros implementation for Kirin

This crate contains the core implementation of derive macros for the Kirin framework. It provides the necessary procedural macros to automatically generate boilerplate code for Kirin components, making it easier to work with the framework.

This crates provides a set of tools to create new kirin derive options, attributes, and helpers for various Kirin traits.
