[package]
name = "kirin-function"
version = "0.1.0"
edition = "2024"

[dependencies]
kirin = { workspace = true }

[dev-dependencies]
kirin-arith = { version = "0.1.0", path = "../kirin-arith" }
kirin-test-utils = { workspace = true, features = ["parser", "pretty"] }
